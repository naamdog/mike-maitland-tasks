{
  "name": "scribbler",
  "version": "1.1.0",
  "private": true,
  "description": "Scribbler — Mike's calm capture-and-do task companion (static PWA + tiny sync/push API)",
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
